const Theme = {
    base_lighter: "#ff8173",
    base: '#FF6A5C',
    base_darker: "#ff4f3f",
    dark: "#414141",
    fresh: "#CCDFCB",
    teal: "#056571",
    white: "#eee"
};

const userTheme = undefined;

const adminTheme = undefined;

const loadTheme = (role) => {
    switch(role) {

        case 'ROLE_ADMIN':

        case 'ROLE_USER':

    }
};

export default Theme;